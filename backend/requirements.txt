# Core
fastapi==0.115.6
starlette==0.41.3
uvicorn[standard]==0.34.0
python-multipart

# Settings & Env
pydantic
pydantic-settings

# Auth & Security
python-jose[cryptography]
passlib[bcrypt]

# Database
supabase
websockets>=14

# AI / LLM (Groq only — removed openai and google-genai)
langchain
langchain-groq
langgraph

# Vector DB
# Pin >=1.0.0 — versions <0.5.0 used np.float_ which was removed in NumPy 2.0,
# causing an AttributeError crash at import time on Render (and any env with NumPy 2.x).
chromadb>=1.0.0
numpy>=1.26,<3

# HTTP
httpx